# gradus ledger — minimal machine state. NO `freeze` field (computed on read).
invariants:
  INV-1: {gate_test_status: covered}    # covered | missing
  INV-2: {gate_test_status: covered}
  INV-3: {gate_test_status: covered}
  INV-4: {gate_test_status: covered}
  INV-5: {gate_test_status: covered}
  INV-6: {gate_test_status: covered}
  INV-7: {gate_test_status: covered}
  INV-8: {gate_test_status: covered}
  INV-9: {gate_test_status: covered}
  INV-10: {gate_test_status: covered}
  INV-11: {gate_test_status: covered}
  INV-12: {gate_test_status: covered}
resolutions:                             # INV-x: {resolved_at_date: "YYYY-MM-DD", note: "..."}
  INV-1: {resolved_at_date: "2026-07-05", note: "project_data() allowlist (SAFE_DATA_KEYS); gate test_payload_data_is_safe_allowlist + real-data PII sweep of .state/snapshot.json both clean. 2026-07-08: error field also sanitized (Fable F2) — persisted error held to the plain provider message (raw --debug payload diverted to a non-persisted debug_detail) + hard 200-char cap in build_snapshot_payload; gate test_payload_error_carries_no_raw_payload. area broadened to providers.py. 2026-08-04: history accepts only validated schema-v2 payloads and fixed safe provenance descriptors; raw/debug/credential-like fields are rejected by tests/test_history.py."}
  INV-2: {resolved_at_date: "2026-07-05", note: "set_headless gate on providers.py choke-points; gate test_write_snapshot_is_read_only_no_side_effects + HeadlessReadOnlyTests assert 0 subprocess.Popen/run across all 5 providers incl. 401 recovery. Antigravity ok:false headless (Keychain subprocess gated); cache READS allowed. 2026-08-04: history queries remain read-only and snapshot journaling starts only after schema-v2 read-back; history failure cannot roll back a committed snapshot."}
  INV-6: {resolved_at_date: "2026-08-04", note: "All credential/secret writes (3 provider caches + Codex auth.json + /tmp debug dump) routed through _write_private (tempfile.mkstemp born 0600 in a 0700 dir, atomic os.replace — no world-readable window, closing the prior Codex write-then-chmod gap); one-time chmod of the 3 pre-existing 0644 caches + non-headless chmod-on-read self-heal for legacy files. Gate test_credential_artifacts_are_written_private. Remediates Fable F1. Credential-free history partitions and its lock are additionally hardened to 0600 inside a 0700 directory."}
  INV-3: {resolved_at_date: "2026-07-13", note: "Cursor v1 remains numeric billing_cycle-only; v2 emits independent numeric ac/ap pools, omitting unavailable values. Both schemas reject bool/non-finite/out-of-range percentages and retain priors only for the matching schema/path."}
  INV-4: {resolved_at_date: "2026-07-13", note: "All observable low-usage indicators use the canonical exact-zero or pace_delta < -0.10 predicate. Malformed sibling metadata is fault-isolated and stale reset parsing cannot manufacture an ahead/behind pace signal."}
  INV-5: {resolved_at_date: "2026-08-04", note: "v1 snapshot.json and v2 snapshot-v2.json are atomically written independently (v1 first); review-plugin and Hermes migrated to v2 with documented v1 rollback and Hermes low-state schema seeding. History uses an independent history_schema_version and preserves the committed v2 snapshot without changing router schemas; retention, timestamp ordering, and read-only query behavior are covered by tests/test_history.py."}
  INV-7: {resolved_at_date: "2026-08-07", note: "2026-08-07: advanced after the existing GradusMacTests/INV7Tests.swift gate passed its mutation proof. publisherSourceReferencesNoCredentialPath rejects hardcoded credential-path references, while snapshotPathHasExactlyOneInjectionPoint rejects a second NSHomeDirectory() call site or a broken start(snapshotPath:) injection; this is the architectural tripwire, not blanket filesystem confinement."}
  INV-8: {resolved_at_date: "2026-08-04", note: "Explicit --refresh-snapshot observer is covered by TestCredentialAwareRefresh::test_refresh_is_explicit_single_flight_progress_visible_and_one_probe: one Agy probe, synthetic schema-v2 projection, safe progress, overlap/lock handling, and machine-safe command separation."}
  INV-9: {resolved_at_date: "2026-08-07", note: "2026-08-07: app/test-gate.sh runs the producer and consumer legs plus test-archive-upload-ios.sh; the archive guard rejects absent, mismatched, wrong-build, or stale machine-written producer evidence before next-ios-build-number.py. Its deleted-evidence-write mutation proof discriminates an upload proceeding without a current producer record."}
  INV-11: {resolved_at_date: "2026-08-07", note: "2026-08-07: app/test-gate.sh now counts the GradusKit, pytest, GradusMac, and both GradusiOS legs, and app/test-gate-selfcheck.sh proves zero, absent, and below-floor reports fail. The snapshot fixture gate also rejects the historical api-for-ap drift and duplicate baselines, discriminating coverage that runs successfully while proving nothing."}
  INV-12: {resolved_at_date: "2026-08-07", note: "2026-08-07: the existing app/test-gate.sh executes the GradusiOS iPhone leg and the separate iPad GradusiOSUITests leg; DensityLayoutXCUITests checks the same provider windows on both paths. This discriminates size-class divergence where one device shows different information or loses the workflow while the other remains green."}
