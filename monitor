#!/bin/sh
""":" # Shell/Python polyglot — finds the project venv automatically
exec "$(dirname "$0")/.venv/bin/python3" -m gradus "$@"
"""
"""Convenience wrapper for launching the monitor (delegating to gradus)."""

from gradus.__main__ import main


if __name__ == "__main__":
    raise SystemExit(main())
