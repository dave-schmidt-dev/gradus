# tasks

> Live queue for current/pending/future work only — never history. Completed work goes to `HISTORY.md`; `done` rows are ported and deleted in the same change.

- [ ] Add `--web` mode to serve the dashboard as a local HTML page.
- [ ] Add system tray / menu bar integration.
- [ ] **(Parked) Antigravity premium-model credits (Opus, gpt-oss).** Not probeable via REST: metered as Codeium-style AI credits over the local language-server gRPC path, authenticated by `agy`'s Keychain credential — no JSON endpoint replicable with the existing provider pattern. **Revisit only if** Google exposes a REST credits endpoint, or it becomes worth a separate gRPC spike (needs `exa.*` protos + Keychain token extraction). Status: parked.
- [ ] **Codex 401 recovery: misleading "session expired" on a post-refresh 5xx.** When `_refresh_tokens()` succeeds but the immediate retry `_request_usage()` hits a transient 5xx, `fetch()` (`ai_monitor/providers.py:1096-1106`) drops into the reload fallback, finds the on-disk token unchanged (refresh already wrote it), and surfaces `Codex session expired: run codex to re-authenticate` — misleading, since the creds are valid and the server merely errored. **Low impact:** no data loss, no extra HTTP call (the unchanged-token guard at :1105 short-circuits before the retry). **Fix:** skip the reload fallback when refresh already succeeded, and distinguish a 5xx from an auth failure. Found during the 2026-06-24 ship review.
- [ ] **Hermes budget-snapshot publisher → use the multi-endpoint SSH-alias fallback pattern when built.** When the planned `budget-snapshot.json` publisher is built, do NOT hardcode a single SSH alias or host. Reuse the LAN-first / tailnet-fallback pattern from the `hermes-publisher` project (v0.3.0): try the LAN alias first, fall back to the tailnet alias, per-endpoint connect timeout, iterate-and-fallback in the ship path, and raise the LAST exception on total failure so callers' `except` clauses still work. **Why this matters:** a single-alias config silently lost several days of envelope flow when the tailnet GUI wasn't running after a reboot — see the `hermes-publisher` postmortem in that project's local docs. **Done when:** the budget-snapshot push succeeds via LAN with the tailnet down, AND via tailnet when off-LAN, AND logs record which endpoint won.
