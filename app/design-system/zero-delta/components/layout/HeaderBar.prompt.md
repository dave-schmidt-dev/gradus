The single header line above any Gradus dashboard.

```jsx
<HeaderBar updatedAt="Mar 13 08:30:14" refresh="57s" />
```

- Three fields separated by `  |  `; nothing else belongs here.
- During a refresh the countdown is replaced by `updating …` in place.
