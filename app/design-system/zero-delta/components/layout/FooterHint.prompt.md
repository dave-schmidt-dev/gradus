Keyboard hint line for terminal surfaces.

```jsx
<FooterHint keys={[{key:'q',action:'quit'},{key:'r',action:'refresh'},{key:'1',action:'fix Codex'}]} />
```

- Key names are single characters in square brackets, lowercase actions after them.
- Auth-recovery keys are appended after the standing q/r pair.
