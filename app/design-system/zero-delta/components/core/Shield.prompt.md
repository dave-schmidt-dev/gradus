Two-tone label/value badge. The identity protocol requires these instead of comma-separated skill or metadata lists.

```jsx
<Shield label="schema" value="v2" />
<Shield label="providers" value="7" tone="ok" />
```

- Label lowercase, value short. Group them in a flex row with an 6-8px gap.
- Never use one as a button.
