Mini icon that grounds a claim: every listed company, school or provider gets one.

```jsx
<MiniIcon src="assets/icons/usmc.png" alt="United States Marine Corps emblem" size={16} />
```

- 16px inline, 32px in list rows; always `object-fit: contain`.
- Ship a local file; do not call a favicon service at render time.
